# The only permitted public production origin.  Deployment and verification
# tooling must read this file; they must never provide a legacy fallback.
PRODUCTION_ORIGIN_HOST=8.217.62.226
PRODUCTION_ORIGIN_USER=root
PRODUCTION_ORIGIN_APP_DIR=/opt/crypto-bot
