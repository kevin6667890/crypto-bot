import {useMemo,useState} from "react";import type {Reconciliation} from "./research";import {useLanguage} from "./i18n";
export default function ReconciliationPanel({data}:{data:Reconciliation|null}){const {t,value}=useLanguage(),[filter,setFilter]=useState("ALL");const items=useMemo(()=>data?.items.filter(item=>filter==="ALL"||filter==="MATCHED"&&item.match_status==="Exact Match"||filter==="UNMATCHED"&&item.match_status!=="Exact Match"||item.match_status===filter)||[],[data,filter]);if(!data)return <div className="research-empty compact">{t("reconciliation.required")}</div>;return <><div className="drift-head"><span className={`drift ${data.drift_status.toLowerCase().replace(" ","-")}`}>{value(data.drift_status)}</span><select value={filter} onChange={e=>setFilter(e.target.value)}><option value="ALL">{t("reconciliation.allLineage")}</option><option value="MATCHED">{t("reconciliation.matched")}</option><option value="UNMATCHED">{t("reconciliation.divergent")}</option></select></div><div className="reconciliation-grid">{[[t("reconciliation.matched"),data.signal_match_rate],[t("common.gap"),data.action_mismatch_rate],["Entry Δ",data.median_entry_difference_pct],[t("reconciliation.divergent"),data.unmatched_signal_ratio]].map(([label,metric])=><article key={String(label)}><span>{label}</span><b>{typeof metric==="number"?`${metric.toFixed(2)}%`:t("common.insufficientData")}</b></article>)}</div><div className="research-table-wrap"><table><thead><tr>{["Signal ID",t("common.instrument"),t("common.time"),"Paper / Backtest",t("common.score"),"Entry Δ","Exit",t("common.result"),t("common.status")].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{items.map((x,i)=><tr key={`${x.signal_id||"legacy"}-${i}`}><td title={x.signal_id}>{x.signal_id?x.signal_id.slice(0,12):"—"}</td><td>{x.instrument}</td><td>{x.candle_close_ts?new Date(x.candle_close_ts*1000).toLocaleString():"—"}</td><td>{value(x.paper_action)||"—"} / {value(x.backtest_action)||"—"}</td><td>{x.paper_score??"—"} / {x.backtest_score??"—"}</td><td>{x.entry_difference_pct!==undefined?`${x.entry_difference_pct.toFixed(3)}%`:"—"}</td><td>{value(x.paper_exit_reason)||"—"} / {value(x.backtest_exit_reason)||"—"}</td><td>{x.paper_result_r?.toFixed(2)??"—"}</td><td>{value(x.match_status)}</td></tr>)}</tbody></table></div></>}
