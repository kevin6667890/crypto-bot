export default function PromotionChecklist({evaluation}:{evaluation:any}){if(!evaluation)return <div className="research-empty compact"><strong>Not evaluated</strong><span>Insufficient evidence for promotion.</span></div>;const result=evaluation.result||evaluation;return <><div className="check-summary"><span className="positive">Passed {result.passed}</span><span className="negative">Failed {result.failed}</span><span>Insufficient {result.insufficient}</span></div><div className="research-table-wrap"><table><thead><tr><th>Requirement</th><th>Status</th><th>Value</th><th>Threshold</th><th>Evidence</th></tr></thead><tbody>{result.checks?.map((x:any)=><tr key={x.key}><td>{x.key.replaceAll('_',' ')}</td><td className={x.status==='passed'?'positive':x.status==='failed'?'negative':''}>{x.status}</td><td>{typeof x.value==='object'?JSON.stringify(x.value):String(x.value??'—')}</td><td>{String(x.threshold??'—')}</td><td>{x.evidence||'—'}</td></tr>)}</tbody></table></div><div className={`research-alert ${result.evidence_sufficient?'success':'warning'}`}>{result.message}</div></>}
