import { useState } from "react";import { StrategyParameters } from "../research";import GateFunnel from "./GateFunnel";import NearMissPanel from "./NearMissPanel";import SensitivityLab from "./SensitivityLab";import BenchmarkPanel from "./BenchmarkPanel";import RobustnessPanel from "./RobustnessPanel";
export default function ValidationWorkspace(props:{startDate:string;endDate:string;parameters:StrategyParameters}){const [tab,setTab]=useState("gates");const tabs=[['gates','Gate Funnel'],['near','Near Miss'],['sensitivity','Sensitivity'],['benchmarks','Benchmarks'],['robustness','Robustness']];return <><section className="research-command"><div><span className="eyebrow">Phase 4 · deterministic validation</span><h1>Strategy Validation</h1><p>Stability, rejection mechanics and counterfactual evidence. No threshold is changed automatically.</p></div></section><div className="validation-tabs">{tabs.map(([key,label])=><button className={tab===key?'active':''} onClick={()=>setTab(key)} key={key}>{label}</button>)}</div>{tab==='gates'?<GateFunnel/>:tab==='near'?<NearMissPanel/>:tab==='sensitivity'?<SensitivityLab {...props}/>:tab==='benchmarks'?<BenchmarkPanel {...props}/>:<RobustnessPanel/>}</>}
